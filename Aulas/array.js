var num = [6, 8, 9, 5, 8];

/*num.sort()
num.push(52);

console.log(`Vetor ${num}`);



*/

//utilizando for para mostrar os valores
/*for (let pos = 0;pos < num.length;pos++) {
   
    
}

for (var pos in num) {
     console.log(num[pos]);
        
    }
*/
    //Buscar valores con indexOf

   let posicao = num.indexOf(10);
   console.log(`O valor 8 está na posição ${posicao}`);


